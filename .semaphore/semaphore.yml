version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: "Clone repo"
    task:
      jobs:
      - name: Big one
        commands:
        - checkout
        - podman --version
        - python3 --version
        - ls -la
        - pwd
        - podman build -t CD-ProJekT-UTM .
        - ls -la
        - pwd
